name: ournet_core
description: UI-independent accountable sharing and replication.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.12.0
dependencies:
  cryptography: ^2.9.0
  crypto: ^3.0.7
  sqlite3: ^3.1.0
  image: 4.5.4
dev_dependencies:
  test: ^1.26.0
  lints: ^6.0.0
